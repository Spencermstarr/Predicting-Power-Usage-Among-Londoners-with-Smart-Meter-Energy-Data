SELECT max(energy_max), avg(energy_max), stddev(energy_max) FROM daily_dataset;

SELECT max(energy_median), avg(energy_median), stddev(energy_median) FROM daily_dataset;

SELECT max(energy_mean), avg(energy_mean), stddev(energy_mean) FROM daily_dataset;


